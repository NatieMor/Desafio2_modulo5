<%= form_with(model: tarea1) do |form| %>
  <% if tarea1.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(tarea1.errors.count, "error") %> prohibited this tarea1 from being saved:</h2>

      <ul>
        <% tarea1.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :descripcion, style: "display: block" %>
    <%= form.text_field :descripcion %>
  </div>

  <div>
    <%= form.label :imagen, style: "display: block" %>
    <%= form.text_field :imagen %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
